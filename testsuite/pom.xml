<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <parent>
        <groupId>org.jboss.resteasy</groupId>
        <artifactId>resteasy-jaxrs-all</artifactId>
        <version>3.1.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <name>RESTEasy Main testsuite</name>
    <description/>
    <modelVersion>4.0.0</modelVersion>
    <packaging>pom</packaging>
    
    <artifactId>resteasy-testsuite</artifactId>

    <properties>
        <version.resteasy.testsuite>${project.version}</version.resteasy.testsuite>
        <additional.surefire.excluded.groups></additional.surefire.excluded.groups>
        <jboss.server.config.file.name>standalone-full.xml</jboss.server.config.file.name>
        <standalone.full.resteasy.legacy.file.name>standalone-resteasy-legacy.xml</standalone.full.resteasy.legacy.file.name>
        <version.org.codehaus.gmaven.gmaven-plugin>1.5</version.org.codehaus.gmaven.gmaven-plugin>
    </properties>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <failIfNoTests>false</failIfNoTests>
                    <systemPropertyVariables>
                        <additionalJvmArgs></additionalJvmArgs>
                        <ipv6>false</ipv6>
                        <ipv6ArquillianSettings></ipv6ArquillianSettings>
                        <node>127.0.0.1</node>
                        <jboss.home>${jboss.home}</jboss.home>
                    </systemPropertyVariables>
                    <excludedGroups>org.jboss.resteasy.category.ExpectedFailing,${additional.surefire.excluded.groups}</excludedGroups>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-install-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>default</id>
            <activation>
                <property>
                    <name>!forward.compatibility</name>
                </property>
            </activation>
            <modules>
                <module>arquillian-utils</module>
                <module>unit-tests</module>
                <module>legacy-integration-tests</module>
                <module>integration-tests</module>
                <module>integration-tests-spring</module>
            </modules>
        </profile>
        <!-- Forward compatibility: use EAP 7.0.0.GA and newer Client from CP stream (e.g. client from RESTEasy delivered by EAP 7.0.2 -->
        <profile>
            <id>forward.compatibility</id>
            <activation>
                <property>
                    <name>forward.compatibility</name>
                </property>
            </activation>
            <modules>
                <module>arquillian-utils</module>
                <module>unit-tests</module>
                <!-- Uncomment module below when EAP/Widlfly with Resteasy 3.1 is released -->
                <!-- <module>legacy-integration-tests</module>-->
                <module>integration-tests</module>
                <module>integration-tests-spring</module>
            </modules>
            <properties>
                <additional.surefire.excluded.groups>org.jboss.resteasy.category.NotForForwardCompatibility</additional.surefire.excluded.groups>
            </properties>
        </profile>
        <profile>
            <id>home</id>
            <activation>
                <property>
                  <name>!jboss.home</name>
                </property>
            </activation>
            <properties>
                <jboss.home>${server.home}</jboss.home>
            </properties>
        </profile>
        <profile>
            <id>server-version</id>
            <activation>
              <property>
                <name>!server.version</name>
              </property>
            </activation>
            <properties>
                <server.version>10.1.0.Final</server.version>
            </properties>
        </profile>
        <profile>
            <id>server-version-9x-exclusions</id>
            <activation>
              <property>
                <name>server.version</name>
                <value>9.0.2.Final</value>
              </property>
            </activation>
            <properties>
              <additional.surefire.excluded.groups>org.jboss.resteasy.category.NotForWildFly9</additional.surefire.excluded.groups>
            </properties>
        </profile>
        <!-- If resteasy.version is specified, it will be used for tests instead of the project.version -->
        <profile>
            <id>custom-resteasy-version</id>
            <activation>
                <property>
                    <name>version.resteasy.testsuite</name>
                </property>
            </activation>
            <properties>
                <version.resteasy.testsuite>${version.resteasy.testsuite}</version.resteasy.testsuite>
            </properties>
        </profile>
        <profile>
            <id>ipv6</id>
            <activation>
                <property>
                    <name>ipv6</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <argLine>${surefireArgLine}</argLine>
                            <testFailureIgnore>true</testFailureIgnore>
                            <systemPropertyVariables>
                                <ipv6>true</ipv6>
                                <ipv6ArquillianSettings>-Djava.net.preferIPv4Stack=false -Djava.net.preferIPv6Addresses=true</ipv6ArquillianSettings>
                                <node>::1</node>
                            </systemPropertyVariables>
                            <excludedGroups>org.jboss.resteasy.category.ExpectedFailing</excludedGroups>
                        </configuration>
                    </plugin>

                </plugins>
            </build>
        </profile>
        <!-- For testing purposes enable to run all tests including the failing ones -->
        <profile>
            <id>enable.expected.failing</id>
            <activation>
                <property>
                    <name>enable.expected.failing</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration combine.children="append">
                            <excludedGroups></excludedGroups>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!-- Specify custom name for jboss server config file. By enabling this profile, this property will be applied to
        the arquillian.xml in all sub-modules running integration tests. As a result, tests using non deprecated api, will
        run with resteasy-legacy module enabled. Sub-module legacy-integration-tests have resteasy-legacy module enabled
        by default, no need enable this profile. -->
        <profile>
            <id>run.server.with.resteasy.legacy.enabled</id>
            <activation>
                <property>
                    <name>run.server.with.resteasy.legacy.enabled</name>
                </property>
            </activation>
            <properties>
                <jboss.server.config.file.name>${standalone.full.resteasy.legacy.file.name}</jboss.server.config.file.name>
            </properties>
            <dependencies>
                <dependency>
                    <groupId>org.jboss.resteasy</groupId>
                    <artifactId>resteasy-legacy</artifactId>
                    <version>${version.resteasy.testsuite}</version>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration combine.children="append">
                            <!-- This test is meant to test  org.jboss.resteasy.spi.ResteasyProviderFactory on the classpath.
                            With this profile enabled org.jboss.resteasy.spi.old.ResteasyProviderFactory is on the classpath
                            and this test would fail. -->
                            <excludes>
                                <exclude>**/ResteasyJAXRSImplTest.java</exclude>
                            </excludes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!-- Create custom resteasy standalone-full.xml file with global module resteasy-legacy added and place it into
        jboss.home directory configuration -->
        <profile>
            <id>groovy</id>
            <activation>
                <property>
                    <name>!no.groovy</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.gmaven</groupId>
                        <artifactId>gmaven-plugin</artifactId>
                        <version>${version.org.codehaus.gmaven.gmaven-plugin}</version>
                        <inherited>false</inherited>
                        <executions>
                            <execution>
                                <id>testsuite-default</id>
                                <phase>process-test-resources</phase>
                                <goals>
                                    <goal>execute</goal>
                                </goals>
                                <configuration>
                                    <providerSelection>2.0</providerSelection>
                                    <source>${basedir}/scripts/legacy.groovy</source>
                                    <properties>
                                        <inputFile>${server.home}/standalone/configuration/standalone-full.xml</inputFile>
                                        <outputFile>${server.home}/standalone/configuration/${standalone.full.resteasy.legacy.file.name}</outputFile>
                                    </properties>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>

